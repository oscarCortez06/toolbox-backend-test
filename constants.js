const bearerTokenKey = 'aSuperSecretKey'

const apiUrl = 'echo-serv.tbxnet.com'

const defaultResponse = {
  error: false,
  message: '',
  results: []
}

module.exports = {
  bearerTokenKey,
  apiUrl,
  defaultResponse
}
